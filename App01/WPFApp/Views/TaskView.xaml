<UserControl x:Class="WPFApp.Views.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <ItemsControl x:Name="IndexList" Margin="10, 10, 10, 10" >
                    </ItemsControl>
                    
                    <ItemsControl x:Name="DeadlineList" Margin="10, 10, 10, 10">
                    </ItemsControl>

                    <ItemsControl x:Name="DescriptionList" Margin="20, 10, 20, 10">
                    </ItemsControl>

                    <ItemsControl x:Name="ConfirmationList" Margin="10, 10, 10, 10">
                    </ItemsControl>
                </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
